---
title: 2x Grid
description:
  The grid is the framework for all the visual elements and typography at IBM.
  It’s fundamental to everything we design.
tabs: ['Overview', 'Implementation', 'Code']
---

<PageDescription>

The Carbon Grid pacakge helps developers build digital experiences with the 2x
Grid. The project provides Grid and Column components which use CSS grid to
build a variety of layouts.

</PageDescription>

<InlineNotification>

If you're using `@carbon/react`, you you probably don't need to install the grid
package separately. See our [Carbon React](/developing/frameworks/react/) guide
to start building.

</InlineNotification>

<AnchorLinks>

<AnchorLink>Documentation</AnchorLink>
<AnchorLink>Getting started</AnchorLink>
<AnchorLink>API & Helpers</AnchorLink>
<AnchorLink>Resources</AnchorLink>

</AnchorLinks>

## Documentation

<Row className="resource-card-group">
  <Column colLg={4} colMd={4} noGutterSm>
  <ResourceCard
    subTitle="React"
    href="https://react.carbondesignsystem.com/?path=/docs/elements-grid--default"
    >

<MdxIcon name="react" />

  </ResourceCard>
</Column>
</Row>

## Getting started

### With `@carbon/styles`

We provide multiple ways for you to use the grid for the Carbon Design System.
By default, the grid code is included when you use `@carbon/styles`. To get
started, you'll need to install this package using `npm`:

```bash
npm install @carbon/styles --save
```

<br />

Or, if you prefer Yarn:

```bash
yarn add @carbon/styles
```

<br />

You can include the grid in one of two ways:

```scss
// If you're including all the styles for Carbon
@use '@carbon/styles';

// Or if you're wanting to use just the grid
@use '@carbon/styles/scss/grid';
```

### Without `@carbon/styles`

If you'd like to use the grid without including `@carbon/styles`, we offer the
`@carbon/grid` package. You can install this with `npm`:

```bash
npm install @carbon/grid --save
```

<br />

Or, if you prefer Yarn:

```bash
yarn add @carbon/grid
```

<br />

You can then include the grid by doing the following in your Sass files:

```scss
@use '@carbon/grid';
```

### FlexGrid

The default grid in v11 is based off of CSS Grid, to enable the previous
[FlexGrid](https://react.carbondesignsystem.com/?path=/docs/elements-flexgrid--auto-columns)
implementation, using Flexbox, you will need to include the following in your
Sass files:

```scss
@use '@carbon/grid';

// Emit the flex-grid styles
@include grid.flex-grid();
```

## API & Helpers

There are many ways to make the 2x Grid work for your application using our API.
The example below illustrates removing visibility of a given component based on
the maximum width of the `md` breakpoint.

```css
.component {
  display: inline;

  /* applies styles inside mixin on md breakpoint or below, like max-width */
  @include breakpoint-down('md') {
    display: none;
  }
}
```

In addition, you can also use Helpers to control what is being displayed on the
screen at a given breakpoint.

```css
@use '@carbon/styles/scss/utilities/helper-classes';

.my-class {
  @include helper-classes.hide-at-sm();
}
```

Checkout the [resources](#resources) section for links to
[the helpers on storybook](https://react.carbondesignsystem.com/?path=/story/helpers-hideatbreakpoint--hide-at-breakpoint)
and the full
[API list on Github](https://github.com/carbon-design-system/carbon/blob/main/packages/grid/docs/sass.md#api).

## Resources

<Row className="resource-card-group">
  <Column colMd={4} colLg={4} noGutterSm>
    <ResourceCard
      subTitle="Carbon grid package"
      href="https://github.com/carbon-design-system/carbon/tree/main/packages/grid"
    >

<MdxIcon name="github" />

  </ResourceCard>
  </Column>
  <Column colMd={4} colLg={4} noGutterSm>
    <ResourceCard
      subTitle="Carbon CSS Grid demo"
      href="https://carbon-elements.netlify.app/grid/examples/css-grid/"
    >

<MdxIcon name="bee" />

  </ResourceCard>
  </Column>
    <Column colMd={4} colLg={4} noGutterSm>
    <ResourceCard
      subTitle="Carbon Flex Grid demo"
      href="https://carbon-elements.netlify.app/grid/examples/preview/"
    >

<MdxIcon name="bee" />

  </ResourceCard>
  </Column>
  <Column colMd={4} colLg={4} noGutterSm>
    <ResourceCard
      subTitle="Carbon grid docs API"
      href="https://github.com/carbon-design-system/carbon/blob/main/packages/grid/docs/sass.md#api"
    >

<MdxIcon name="github" />

  </ResourceCard>
  </Column>
  <Column colMd={4} colLg={4} noGutterSm>
    <ResourceCard
      subTitle="Hide at Breakpoint Helpers on Storybook"
      href="https://react.carbondesignsystem.com/?path=/story/helpers-hideatbreakpoint--hide-at-breakpoint"
    >

<MdxIcon name="storybook" />

  </ResourceCard>
  </Column>
</Row>
